<?xml version="1.0"?>
<ruleset name="SilverAssist GraphQL Shortcode Support">
    <description>WordPress Coding Standards for GraphQL Shortcode Support Plugin</description>

    <!-- What to scan -->
    <file>.</file>

    <!-- Exclude paths -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/.git/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/build/*</exclude-pattern>

    <!-- Show progress and colors -->
    <arg value="ps"/>
    <arg name="colors"/>

    <!-- Check code for cross-version PHP compatibility -->
    <config name="testVersion" value="8.2-"/>

    <!-- Include WordPress-Extra standard -->
    <rule ref="WordPress-Extra">
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>

        <!-- PSR-4 PascalCase filenames, not WordPress hyphenated lowercase -->
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>

        <!-- Allow opening braces on same line (K&R style) -->
        <exclude name="Generic.Functions.OpeningFunctionBraceKernighanRitchie"/>
        <exclude name="Generic.Classes.OpeningBraceSameLine"/>
    </rule>

    <!-- Allow short array syntax explicitly -->
    <rule ref="Universal.Arrays.DisallowShortArraySyntax">
        <exclude name="Universal.Arrays.DisallowShortArraySyntax.Found"/>
    </rule>

    <!-- Enforce WordPress naming conventions -->
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="gss"/>
                <element value="SilverAssist\GraphQLShortcodeSupport"/>
            </property>
        </properties>
    </rule>

    <!-- Enforce WordPress documentation standards -->
    <rule ref="WordPress-Docs"/>

    <!-- Extra rules -->
    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>
    <rule ref="Generic.Commenting.Todo"/>

    <!-- Minimum WordPress version -->
    <config name="minimum_supported_wp_version" value="6.5"/>
</ruleset>
